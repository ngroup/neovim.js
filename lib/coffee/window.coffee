###*
# Initialize a new `Window` with the given `index` and `client`.
# @class Represent a Window
# @param {int} index - The window index
# @param {Client} client - The client
###
Window = (index, client) ->
  @index = index
  @client = client
  return

